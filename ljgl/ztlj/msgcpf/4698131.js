(function () {    var linkLiArr = [],num = ll_4698131;    if (!num || num <= 0) {        num = "5";    }                var printContent = function(){        var length = linkLiArr.length;        if (num > length) {            num = length;        }        if (linkLiArr.length > 0) {            for (var i=0;i < num;i++) {                document.write(linkLiArr[i]);            }        } else {            $("#j-scroll-4698131").hide();        }    }();})()